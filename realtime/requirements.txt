# Realtime Market Data Ingestion Server
# ─────────────────────────────────────

# Async websocket client
websockets>=12.0

# Async HTTP client (Kraken REST API + health check server)
aiohttp>=3.9

# Async Redis client (batched pipeline writes)
# hiredis is an optional C parser that speeds up Redis protocol parsing
redis[hiredis]>=5.0

# Environment variable management
python-dotenv>=1.0
